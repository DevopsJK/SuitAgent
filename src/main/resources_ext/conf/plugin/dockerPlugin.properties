step = 30
# 注:pluginActivateType属性在此插件将不起作用,只要配置了监控地址,就会触发监控启动
# Docker插件会自动判断启动，无需配置地址

# 手动指定cAdvisor的端口地址（若指定次配置，则不会启动内置的CAdvisor和不会自动检查本机是否存在cAdvisor容器。若本机有cAdvisor服务并且SuitAgent自动检测失败，可配置此项）
#cadvisor.port = 8901

# 若需要的话，启动内置的cAdvisor的监听端口地址（此项保持默认开启即可）
cadvisor.start.port = 8901
